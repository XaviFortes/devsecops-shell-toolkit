# Bash support

This folder is reserved for future Bash commands.

Suggested layout:

- `commands/`: reusable shell functions and scripts
- `install.sh`: placeholder Bash installer entry point
- `update.sh`: placeholder Bash updater entry point
- `README.md`: shell-specific usage

Keeping Bash isolated here lets you publish one repo for multiple shells without mixing install logic.
